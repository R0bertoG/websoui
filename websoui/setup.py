from setuptools import setup

setup(
        name = 'websoui'
        , version = '0.1'
        , description = 'Very simple websocket server for using a browser as UI.'
        , author = 'R0bertoG'
        , packages = ['websoui']
        , install_requires = ['websockets']
)
